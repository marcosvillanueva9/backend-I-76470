<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Students</title>
</head>
<body>
    <h1>Students</h1>
    <ul>
        {{#each students}}
            <li>{{this.first_name}} {{this.last_name}}</li>
        {{/each}}
    </ul>

    <p>Página {{currentPage}} de {{totalPages}}</p>

    <div>
        {{#if hasPrevPage}}
            <a href="/students?page={{#if prevPage}}{{prevPage}}{{else}}1{{/if}}&limit={{#if limit}}{{limit}}{{else}}10{{/if}}">Previous</a>
        {{/if}}

        {{#if hasNextPage}}
            <a href="/students?page={{#if nextPage}}{{nextPage}}{{else}}2{{/if}}&limit={{#if limit}}{{limit}}{{else}}10{{/if}}">Next</a>
        {{/if}}
    </div>
</body>
</html>